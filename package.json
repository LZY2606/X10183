{
  "name": "bus-scale",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "总线刻度 - CAN trace 按采集时点重放与信号解释",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
